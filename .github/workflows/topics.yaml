name: Setup kafka topics

on:
  push:
    paths:
      - '.github/workflows/topics.yaml'
      - '.nais/**/*-topic.yaml'

env:
  CI: true
  TZ: Europe/Oslo

jobs:
  henvendelse-topic:
    name: Setup henvendelse topic
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Setup preprod
        if: github.ref == 'refs/heads/dev'
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          PRINT_PAYLOAD: true
          CLUSTER: dev-fss
          RESOURCE: .nais/henvendelse-topic.yaml
          VARS: .nais/vars/preprod.yaml
          DRY_RUN: true
      - name: Setup prod
        if: github.ref == 'refs/heads/master'
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          PRINT_PAYLOAD: true
          CLUSTER: dev-fss
          RESOURCE: .nais/henvendelse-topic.yaml
          VARS: .nais/vars/prod.yaml
          DRY_RUN: true
  hendelse-topic:
    name: Setup henvendelse topic
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Setup preprod
        if: github.ref == 'refs/heads/dev'
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          PRINT_PAYLOAD: true
          CLUSTER: dev-fss
          RESOURCE: .nais/hendelse-topic.yaml
          VARS: .nais/vars/preprod.yaml
          DRY_RUN: true
      - name: Setup prod
        if: github.ref == 'refs/heads/master'
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          PRINT_PAYLOAD: true
          CLUSTER: dev-fss
          RESOURCE: .nais/hendelse-topic.yaml
          VARS: .nais/vars/prod.yaml
          DRY_RUN: true